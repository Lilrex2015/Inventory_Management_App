To launch the local host server

type commands

    npm run build
    node index.js


    the npm run build will launch the webpack bundler
    index.js is the server side script

    url for localhost in chrome is LOCALHOST:3000